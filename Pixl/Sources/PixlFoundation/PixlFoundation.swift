// PixlFoundation owns platform-independent, lower-level engine mechanisms.
//
// It sits beneath Pixl's game-facing conveniences and above PixlPlatform's
// portable input/output contracts. Declarations are package-scoped by default
// and become public only when a deliberate direct-use API requires it.
import PixlPlatform
