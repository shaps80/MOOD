#!/usr/bin/env bash

WEB_TOOLCHAIN="6.4.x-snapshot-2026-07-17"
WEB_SDK="swift-6.4.x-DEVELOPMENT-SNAPSHOT-2026-07-17-a_wasm"
WEB_SDK_URL="https://download.swift.org/swift-6.4.x-branch/wasm-sdk/swift-6.4.x-DEVELOPMENT-SNAPSHOT-2026-07-17-a/swift-6.4.x-DEVELOPMENT-SNAPSHOT-2026-07-17-a_wasm.artifactbundle.tar.gz"
WEB_SDK_CHECKSUM="fb0ed0f75a7d1af3b2b1c1f94f424de58290e02de51778f108a637e42b93b0b2"

ensure_web_toolchain() {
  if ! command -v swiftly >/dev/null 2>&1; then
    echo "error: swiftly is required: https://www.swift.org/install/" >&2
    exit 1
  fi

  if ! swiftly run swift --version +"${WEB_TOOLCHAIN}" >/dev/null 2>&1; then
    echo "Installing Swift ${WEB_TOOLCHAIN}..."
    swiftly install "${WEB_TOOLCHAIN}" --assume-yes
  fi

  if ! swiftly run swift sdk list +"${WEB_TOOLCHAIN}" | grep -Fxq "${WEB_SDK}"; then
    echo "Installing Swift WebAssembly SDK ${WEB_SDK}..."
    swiftly run swift sdk install \
      "${WEB_SDK_URL}" \
      --checksum "${WEB_SDK_CHECKSUM}" \
      +"${WEB_TOOLCHAIN}"
  fi
}
